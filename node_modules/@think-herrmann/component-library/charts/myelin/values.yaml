env: development
commit: localDev

domain: localhost

image:
    org: axon
    nginx:
        name: nginx
    app:
        name: components
    tag: sandbox
    pullPolicy: Never

autoscaler:
    enabled: false
    minReplicaCount: 2
    maxReplicaCount: 8
