---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-public
  labels:
    apps: {{ .Chart.Name }}-public
spec:
  type: NodePort
  ports:
    - name: {{ .Chart.Name }}-public
      protocol: TCP
      port: 80

  selector:
    app: {{ .Chart.Name }}-public
    env: {{ .Values.env }}
